export interface createCourseDTO {
  discription: string;
  totalTime: string;
  startLocation: string;
  endLocation: string;
  hashtag: string;
  music: string;
  path: number;
}

export interface createPhotoDTO {
  photoUrl: string;
}